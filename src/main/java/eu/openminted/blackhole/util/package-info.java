/**
 * Subset of Google Guava (version 17) copied into library (and modified slightly) 
 * so the functionality is available but we don't actually need to depend on another
 * library.
 * 
 */
package eu.openminted.blackhole.util;
